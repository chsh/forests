require 'spec_helper'

describe SiteViewController do

  #Delete this example and add some real ones
  it "should use SiteViewController" do
    controller.should be_an_instance_of(SiteViewController)
  end

#  it 'should return 404 if site does not exist.' do
#    get '/unknown-site/not-existent-page.html'
#    response.should redirect_to(error_path(404))
#  end

end
